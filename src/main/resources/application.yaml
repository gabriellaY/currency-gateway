spring:
  application:
    name: CurrencyGateway
  profiles:
    active: mysql
  data:
    redis:
      url: redis://localhost:6379
      host: localhost
      port: 6379
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/currency_info_gateway
    username: root
    password: 12345678
    initialize: true
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          show_sql: false
          use_sql_comments: true
          format_sql: true
          type: trace
  logging:
    level:
      org.hibernate: TRACE
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    template:
      exchange: amq.topic
      routingKey: currency.gateway.api-requests
      receiveQueue: api-requests-queue

currency-gateway:
  fixer:
    apiAccessKey: 9536cbefbb6c172a961d4943bece59d4
    baseUrl: http://data.fixer.io/api
    latestRatesUrl: /latest
    currenciesUrl: /symbols
  schedules:
    rates-collector: "0 0 0,12 * * ?"